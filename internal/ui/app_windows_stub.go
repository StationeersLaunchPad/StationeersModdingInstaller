//go:build windows

package ui

import "fmt"

// Run is a stub; the real app is built from the root package using Wails.
func Run() {
	fmt.Println("Run the Wails build from the project root: wails build")
}
